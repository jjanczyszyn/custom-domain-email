{
  "name": "email-forwarder",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "description": "SES inbound -> Gmail forwarder. AWS SDK v3 is provided by the nodejs20.x runtime, so there are no dependencies to bundle.",
  "dependencies": {
    "jimp": "^1.6.1",
    "mailparser": "^3.9.12",
    "nodemailer": "^7.0.13"
  }
}
